#ifndef TUTILS_H_
#define TUTILS_H_


// Darf nur innerhalb einer .cpp Datei eingebunden werden!!!! Sonst streikt QList
#define min(a, b) (((a) < (b)) ? (a) : (b))
#define max(a, b) (((a) > (b)) ? (a) : (b))

#endif /*TUTILS_H_*/
